# mtc_task_manager

负责人：SJM。状态：目录骨架，唯一任务状态机尚未实现。

维护基础/序列任务口令解析、状态机、目标选择、阶段超时、有界重试和恢复。输入 task、观测、执行反馈；输出抓放动作请求和阶段事件。不得绕过执行层直接给关节控制器发送互相冲突的命令。

常建烁负责 `mtc_motion_planning` 的轨迹生成和规划结果；本模块根据规划/执行失败原因决定下一步任务行为。

预留 `mtc_task_manager/`、`config/`、`launch/`、`test/`，建议 Python。验收包括四种基础颜色、24 种三色排列、无目标/旧观测/动作取消/执行失败等分支，且错误能明确定位到阶段。
