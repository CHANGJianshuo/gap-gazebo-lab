# mtc_calibration

负责人：韩明赫。状态：目录骨架，标定与转换节点尚未实现。

维护内参/对齐配置、腕部手眼、TCP、底图注册和图像时刻 TF 查询；将相机观测转换成基座位姿并评估空间误差。仿真已知安装变换与真实标定测量结果要分别标记。

预留 `mtc_calibration/`、`config/`、`launch/`、`test/`，建议 Python + tf2。验收报告覆盖工作区多个点的均值、中位数、P95、最大误差和 yaw 误差；校验随腕部运动的变换及观测过期处理。本阶段不实现 SLAM。
