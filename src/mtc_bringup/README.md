> 本次规划演示的一键启动已由 scripts/run_demo.sh 提供；此包仍预留给张叔阳整合全队正式启动。

# mtc_bringup

负责人：张叔阳。状态：目录骨架，一键启动和健康监测尚未实现。

统一启动 simulation/hardware、相机、TF、感知、规划、执行、任务和日志，管理 use_sim_time、启动顺序、命名空间和配置版本。系统就绪前不得开始比赛动作。

预留 `launch/`、`config/`、`mtc_bringup/`。验收应能从干净终端启动、重复退出/复位，定位相机无帧、TF 不可用、控制器未激活和节点退出。未来启动命令实现后再写入顶层 README，当前不提供不存在的 launch 命令。
